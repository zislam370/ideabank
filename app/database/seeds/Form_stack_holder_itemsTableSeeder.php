<?php

class Form_stack_holder_itemsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_stack_holder_items')->truncate();

		$form_stack_holder_items = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_stack_holder_items')->insert($form_stack_holder_items);
	}

}
