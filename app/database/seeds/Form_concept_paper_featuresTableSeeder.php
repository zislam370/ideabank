<?php

class Form_concept_paper_featuresTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_concept_paper_features')->truncate();

		$form_concept_paper_features = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_concept_paper_features')->insert($form_concept_paper_features);
	}

}
