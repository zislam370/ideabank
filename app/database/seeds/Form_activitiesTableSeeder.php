<?php

class Form_activitiesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_activities')->truncate();

		$form_activities = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_activities')->insert($form_activities);
	}

}
