<?php

class Form_evaluation_itemsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_evaluation_items')->truncate();

		$form_evaluation_items = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_evaluation_items')->insert($form_evaluation_items);
	}

}
