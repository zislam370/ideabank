<?php

class Form_budgetsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_budgets')->truncate();

		$form_budgets = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_budgets')->insert($form_budgets);
	}

}
