<?php

class Form_lookupsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_lookups')->truncate();

		$form_lookups = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_lookups')->insert($form_lookups);
	}

}
