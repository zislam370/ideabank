<?php

class Form_evaluationsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_evaluations')->truncate();

		$form_evaluations = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_evaluations')->insert($form_evaluations);
	}

}
