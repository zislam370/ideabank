<?php

class Form_visitsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_visits')->truncate();

		$form_visits = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_visits')->insert($form_visits);
	}

}
