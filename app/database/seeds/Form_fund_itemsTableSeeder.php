<?php

class Form_fund_itemsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_fund_items')->truncate();

		$form_fund_items = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_fund_items')->insert($form_fund_items);
	}

}
