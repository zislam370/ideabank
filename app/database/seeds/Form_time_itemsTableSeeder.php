<?php

class Form_time_itemsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_time_items')->truncate();

		$form_time_items = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_time_items')->insert($form_time_items);
	}

}
