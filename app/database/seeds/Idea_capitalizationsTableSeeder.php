<?php

class Idea_capitalizationsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('idea_capitalizations')->truncate();

		$idea_capitalizations = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('idea_capitalizations')->insert($idea_capitalizations);
	}

}
