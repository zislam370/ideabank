<?php

class Form_deliverable_itemsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_deliverable_items')->truncate();

		$form_deliverable_items = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_deliverable_items')->insert($form_deliverable_items);
	}

}
