<?php

class Form_fundsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_funds')->truncate();

		$form_funds = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_funds')->insert($form_funds);
	}

}
