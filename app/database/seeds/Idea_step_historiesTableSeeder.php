<?php

class Idea_step_historiesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('idea_step_histories')->truncate();

		$idea_step_histories = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('idea_step_histories')->insert($idea_step_histories);
	}

}
