<?php

class Idea_stepsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('idea_steps')->truncate();

		$idea_steps = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('idea_steps')->insert($idea_steps);
	}

}
