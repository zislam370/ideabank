<?php

class Form_timesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_times')->truncate();

		$form_times = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_times')->insert($form_times);
	}

}
