<?php

class Form_payment_schedulesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_payment_schedules')->truncate();

		$form_payment_schedules = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_payment_schedules')->insert($form_payment_schedules);
	}

}
