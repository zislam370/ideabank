<?php

class Activity_formsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('activity_forms')->truncate();

		$activity_forms = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('activity_forms')->insert($activity_forms);
	}

}
