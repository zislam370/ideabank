<?php

class Workflow_categoriesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('workflow_categories')->truncate();

		$workflow_categories = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('workflow_categories')->insert($workflow_categories);
	}

}
