<?php

class Workflow_step_activitiesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('workflow_step_activities')->truncate();

		$workflow_step_activities = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('workflow_step_activities')->insert($workflow_step_activities);
	}

}
