<?php

class Form_payment_schedule_itemsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_payment_schedule_items')->truncate();

		$form_payment_schedule_items = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_payment_schedule_items')->insert($form_payment_schedule_items);
	}

}
