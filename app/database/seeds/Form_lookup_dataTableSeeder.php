<?php

class Form_lookup_dataTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_lookup_data')->truncate();

		$form_lookup_data = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_lookup_data')->insert($form_lookup_data);
	}

}
