<?php

class Form_visit_itemsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_visit_items')->truncate();

		$form_visit_items = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_visit_items')->insert($form_visit_items);
	}

}
