<?php

class Sif_roundsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('sif_rounds')->truncate();

		$sif_rounds = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('sif_rounds')->insert($sif_rounds);
	}

}
