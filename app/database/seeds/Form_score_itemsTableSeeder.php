<?php

class Form_score_itemsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_score_items')->truncate();

		$form_score_items = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_score_items')->insert($form_score_items);
	}

}
